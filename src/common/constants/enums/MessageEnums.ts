enum MessageType {
    Initialize,
    FilePicker,
    WriteVariable,
    WriteEnvironment,
    WriteFunction,
    GetVariable,
    GetEnvironment,
    GetFunction,
    GetRawFunction,
}

export default MessageType;